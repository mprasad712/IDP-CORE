apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: idpflow
type: Opaque
# Data is intentionally empty here.
# All secret values are injected by the CI pipeline from Azure Key Vault.
# ArgoCD manages the Secret object but never writes data (ignoreDifferences on /data).
