apiVersion: v1
kind: Service
metadata:
  name: model-service-svc
  namespace: idpflow
spec:
  selector:
    app: model-service
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
